const mainColor = '#7b62ac';
const cliName = 'pokemon cli';
const cliNameHeader = 'pok\u00E9mon cli'.toUpperCase();
const author = 'mikey';
const authorFormatted = author.charAt(0).toUpperCase() + author.slice(1);

export { mainColor, cliName, cliNameHeader, author, authorFormatted };
